import AddProject from "./AddProject";
import FilterProjects from "./Filter";

const Layout = () => {
  return (
    <div>
      <h1>Portfolio</h1>
      <AddProject />
      <FilterProjects />
    </div>
  );
};

export default Layout;
